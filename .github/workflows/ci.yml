name: CI
on: 
  workflow_call:
jobs: 
  lint: 
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v6
      - run: npm ci
      - run: npm run lint
  component_test:
    runs-on: ubuntu-latest
    needs: lint
    steps: 
        - uses: actions/checkout@v6
        - run: npm ci
        - run: npm test
  build:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v6
        - run: npm ci
        - run: npm run build
  e2e_test:
    runs-on: ubuntu-latest
    needs: build
    steps:
        - name: Checkout
          uses: actions/checkout@v6
        - name: Cypress run
          uses: cypress-io/github-action@v7
          with: 
            build: npx cypress run
            browser: edge